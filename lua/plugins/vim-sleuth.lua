-- [[ vim-sleuth ]]
-- desc: heuristically set buffer options
-- repo: https://github.com/tpope/vim-sleuth

-- INFO: automatically adjusts `shiftwidth` and `expandtab` based on the curren file

return {
  'tpope/vim-sleuth',
}
